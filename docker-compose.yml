services:
  database:
    image: postgres:15
    container_name: postgres-db-reportandil
    environment:
      - POSTGRES_DB=reportandil
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=NZN
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: